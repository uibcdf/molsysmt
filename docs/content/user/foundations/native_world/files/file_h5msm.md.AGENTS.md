# Micro-Governance: `file_h5msm.md` (`file_h5msm.md.AGENTS.md`)

Governs `docs/content/user/foundations/native_world/files/file_h5msm.md`.

## 🔒 Directives
- Title MUST be `# h5msm` (no `file:` prefix).
- MUST preserve top anchor `(user-foundations-native-world-files-file-h5msm)=`.
- Follows the 4-section layout for native files:
  1. `## Overview and Format Purpose`
  2. `## HDF5 Layout and Dataset Schema`
  3. `## Read, Write, and Streaming Operations`
  4. `## Performance and Storage Invariants`
- Covers Schema 0.4 specification and Schema 0.3 backward compatibility.
