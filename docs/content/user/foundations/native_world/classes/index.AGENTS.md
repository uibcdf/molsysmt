# Micro-Governance: `index.md` (`index.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/native_world/classes/index.md`](index.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Classes`.
   - MUST preserve top anchor `(user-foundations-native-world-classes)=` for cross-link stability.

2. **Chapter Navigation Layout**:
   - MUST use a clean bulleted list under `## **Contents**`.
   - MUST NOT use cards (`::::{grid}`) or badges (`{bdg-*}`).

3. **Toctree**:
   - MUST maintain hidden `toctree` with `maxdepth: 1` listing all native class form units.
