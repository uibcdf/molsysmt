# Micro-Governance: `molsysmt_MolSys.md` (`molsysmt_MolSys.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/native_world/classes/molsysmt_MolSys.md`](molsysmt_MolSys.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# molsysmt.MolSys` (plain text without backticks).
   - MUST preserve top anchor `(user-foundations-native-world-molsys)=`.

2. **Form-Centric Pedagogical Scope**:
   - Dedicated EXCLUSIVELY to explaining the `molsysmt.MolSys` native form *per se*.
   - Pure conceptual Markdown page explaining internal architecture (`topology`, `structures`, `molecular_mechanics`) and design invariants.
   - Does NOT include code execution cells, inspection code, or API function tutorials.
