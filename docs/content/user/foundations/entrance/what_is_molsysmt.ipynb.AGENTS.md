# Micro-Governance: `what_is_molsysmt.ipynb` (`what_is_molsysmt.ipynb.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/entrance/what_is_molsysmt.ipynb`](what_is_molsysmt.ipynb).

---

## 🔒 Frozen & Inviolable Content

1. **Mandatory MyST Section Anchor**:
   - `(user-foundations-entrance-what-is-molsysmt)=`

2. **Core Messaging Pillars (Must NOT be removed or weakened)**:
   - **Multi-library Friction & Glue Code Elimination**: Clear statement explaining that computational structural biology suffers from 4-5 incompatible library models and brittle glue code.
   - **Form-Agnostic Paradigm**: Explicit definition of form agnosticism across 89 supported forms (75 Tier-1 stable).
   - **Native Preparation Pipeline**: Native capability to diagnose and repair heavy atoms, cappings, protonation at pH, solvation, and ions without requiring external tools like PDBFixer.
   - **Native Rust Compute**: Precompiled Rust kernels for distances, SASA, RMSD, gyration radius, and dihedrals with 0ms JIT warmup.
   - **Physical Safety (`pyunitwizard`)**: Strict unit management across physical quantities.
   - **MolSysSuite Foundation**: Positioning MolSysMT as the core "Operating System" for molecular data powering TopoMet, ElastNetMT, and MolSysViewer.

3. **Required Code Demonstrations**:
   - Executable Python code cells showing `msm.convert()` loading `1l2y.h5msm`, calling `msm.get()`, and converting to `mdtraj.Trajectory` and `string:amino_acids_1`.

4. **Frozen Admonition**:
   - The `Key Takeaway` admonition box summarizing MolSysMT as the unified Operating System for molecular data.
