# Micro-Governance: `parallelization.md` (`parallelization.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/performance/parallelization.md`](parallelization.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Parallel Execution`.
   - MUST preserve top anchor `(user-foundations-performance-parallelization)=`.

2. **Conceptual Scope**:
   - Explains Rayon multi-core thread distribution, threadpool controls, and SIMD hardware vectorization.
