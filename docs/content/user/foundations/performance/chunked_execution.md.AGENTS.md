# Micro-Governance: `chunked_execution.md` (`chunked_execution.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/performance/chunked_execution.md`](chunked_execution.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Chunked Execution`.
   - MUST preserve top anchor `(user-foundations-performance-chunked-execution)=`.

2. **Conceptual Scope**:
   - Explains memory wall problem, Eager vs Heavy paths, `heavy_mode` controls, custom user iterator scripts (`molsysmt.Iterator`), and `SMonitor` memory pressure monitoring.
