# Micro-Governance: `caching_and_memoization.md` (`caching_and_memoization.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/performance/caching_and_memoization.md`](caching_and_memoization.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Caching & Memoization`.
   - MUST preserve top anchor `(user-foundations-performance-caching-and-memoization)=`.

2. **Conceptual Scope**:
   - Explains selection AST caching, index caching, registry memoization, and lazy module mapping.
