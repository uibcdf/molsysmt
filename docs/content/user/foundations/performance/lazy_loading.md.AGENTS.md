# Micro-Governance: `lazy_loading.md` (`lazy_loading.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/performance/lazy_loading.md`](lazy_loading.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Lazy Loading`.
   - MUST preserve top anchor `(user-foundations-performance-lazy-loading)=`.

2. **Conceptual Scope**:
   - Explains deferred evaluation, metadata-first initialization, and streaming I/O.
   - Does NOT include code execution cells or function API tutorials.
