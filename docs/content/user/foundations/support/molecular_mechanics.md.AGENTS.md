# Micro-Governance: `molecular_mechanics.md` (molecular_mechanics.md.AGENTS.md)

Governs `docs/content/user/foundations/support/molecular_mechanics.md`.

## 📜 Editorial Rules & Layout Standards
- **Section Layout**: Must maintain 3 distinct H2 sections (`## Forcefields and Water Models`, `## Non-Bonded and Continuum Solvent Settings`, `## Constraints and Integration Parameters`).
- **Table Formatting**: Each section MUST present a 4-column left-aligned table (`Attribute / Property`, `Scope / Options`, `Description`, `Canonical Unit`).
- **Attribute Naming**: Attribute keys MUST be formatted as code blocks (e.g. `` `forcefield` ``).
- **Physical Units**: Physical units MUST be explicitly stated using PyUnitWizard standard canonical units (`nm`, `Da`, `e`, `M`).
- **No Functions**: API function tutorials belong under `docs/content/user/tools/`.
