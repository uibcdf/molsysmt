# Micro-Governance: `viewers.md` (viewers.md.AGENTS.md)

Governs `docs/content/user/foundations/support/viewers.md`.

## 📜 Editorial Rules & Layout Standards
- **Section Layout**: Must feature `## Supported 3D Rendering Backends` with a 4-column left-aligned matrix table (`Viewer Backend`, `Description`, `Environment`, `Support Level`).
- **Backend Naming**: Backend names MUST match official identifiers (`MolSysViewer`, `NGLView`, `Py3Dmol`).
- **Configuration References**: Configuration parameters MUST reference `msm.configure.default_viewer`.
