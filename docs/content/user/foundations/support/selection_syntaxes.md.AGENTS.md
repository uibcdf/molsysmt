# Micro-Governance: `selection_syntaxes.md` (selection_syntaxes.md.AGENTS.md)

Governs `docs/content/user/foundations/support/selection_syntaxes.md`.

## 📜 Editorial Rules & Layout Standards
- **Section Layout**: Must feature `## Supported Syntaxes and Shortcuts` with a 4-column left-aligned matrix table (`Syntax`, `Selection Input`, `Translation Output`, `Scope`).
- **Directional Clarity**: Must explicitly distinguish between selection input (`syntax=...`) and selection translation (`to_syntax=...`).
- **Code Formats**: Syntax names and shortcuts MUST be formatted as code blocks (e.g. `` `MolSysMT` ``, `` "backbone" ``).
