# Micro-Governance: `precision_and_types.md` (`precision_and_types.md.AGENTS.md`)

This micro-governance contract governs [`docs/content/user/foundations/governance/precision_and_types.md`](precision_and_types.md).

---

## 🔒 Directives

1. **Title & MyST Anchor**:
   - Title MUST be `# Numeric Precision & Data Standards`.
   - MUST preserve top anchor `(user-foundations-governance-precision-and-types)=`.

2. **Conceptual Scope**:
   - Explains float32/float64 precision policies, array shape invariants, and string ID normalization invariants (*_id as strings).
