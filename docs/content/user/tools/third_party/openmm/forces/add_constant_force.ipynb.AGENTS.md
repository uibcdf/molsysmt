# Micro-Governance: add_constant_force.ipynb

## Purpose
Governance rules and frozen contracts for `add_constant_force.ipynb`.

## Cell Sequence & Inviolable Order
1. **Cell 1 (Code, metadata `{"tags": ["remove-input"]}`)**: Warning suppression.
2. **Cell 2 (Markdown - Header Block)**: Anchor `(Tutorial_OpenMM_Add_constant_force)=`, title `# Add constant force`, gerund summary, versionadded, and collapsible API documentation dropdown `{func}`molsysmt.third_party.openmm.forces.add_constant_force``.
3. **Cell 3 (Markdown - First Real H2 Opener)**: `## Basic usage`.
4. **Cells 4+ (Code/Markdown)**: Minimal, deterministic, executable code examples using MolSysMT.
5. **Final Cell (Markdown)**: Collapsible `{seealso}` dropdown with contextual links.
