# Micro-Governance: show_as_balls_and_sticks.ipynb

## Purpose
Governance rules and frozen contracts for `show_as_balls_and_sticks.ipynb`.

## Cell Sequence & Inviolable Order
1. **Cell 1 (Code, metadata `{"tags": ["remove-input"]}`)**: Warning suppression.
2. **Cell 2 (Markdown - Header Block)**: Anchor `(Tutorial_NGLView_Show_as_balls_and_sticks)=`, title `# Show as balls and sticks`, gerund summary, versionadded, and collapsible API documentation dropdown `{func}`molsysmt.third_party.nglview.show_as_balls_and_sticks``.
3. **Cell 3 (Markdown - First Real H2 Opener)**: `## Basic usage`.
4. **Cell 4 (Code)**: Setup and operation execution.
5. **Cell 5 (Markdown)**: Evaluating `view` opener.
6. **Cell 6 (Code, metadata `{"tags": ["remove-output"]}`)**: `view` object.
7. **Cell 7 (Code, metadata `{"tags": ["remove-input"]}`)**: Embedded HTML iframe loader.
8. **Final Cell (Markdown)**: Collapsible `{seealso}` dropdown with contextual links.
