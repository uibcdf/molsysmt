# Micro-Governance: load_html_in_jupyter_notebook.ipynb

## Purpose
Governance rules and frozen contracts for `load_html_in_jupyter_notebook.ipynb`.

## Cell Sequence & Inviolable Order
1. **Cell 1 (Code, metadata `{"tags": ["remove-input"]}`)**: Warning suppression.
2. **Cell 2 (Markdown - Header Block)**: Anchor `(Tutorial_NGLView_Load_html_in_jupyter_notebook)=`, title `# Load HTML in Jupyter Notebook`, gerund summary, versionadded, and collapsible API documentation dropdown `{func}`molsysmt.third_party.nglview.load_html_in_jupyter_notebook``.
3. **Cell 3 (Markdown - First Real H2 Opener)**: `## Basic usage`.
4. **Cells 4+ (Code/Markdown)**: Minimal, deterministic, executable code examples using MolSysMT.
5. **Final Cell (Markdown)**: Collapsible `{seealso}` dropdown with contextual links.
