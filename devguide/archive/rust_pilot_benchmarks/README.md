# Rust Adoption Pilot Benchmarks

These scripts are historical evidence from the Numba-to-Rust comparison pilot.
They deliberately import or reference Numba and no longer describe an
executable MolSysMT runtime contract.

Do not use them for current release measurements. The maintained Rust-only
release benchmark is `benchmarks/rust/bench_release_runtime.py`.
